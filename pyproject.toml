[project]
name = "ask-o11y-analysis-platform"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "opensandbox==0.1.15",
    "opensandbox-code-interpreter==0.1.2",
]

[tool.uv]
package = false
