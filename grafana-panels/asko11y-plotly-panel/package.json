{
  "name": "asko11y-plotly-panel",
  "version": "0.3.1",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && rm -rf dist && webpack --config webpack.config.cjs && node scripts/remove-dynamic-global.cjs && cp src/plugin.json dist/plugin.json && mkdir -p dist/img && cp src/img/logo.svg dist/img/logo.svg"
  },
  "dependencies": {
    "plotly.js-dist-min": "3.1.0"
  },
  "devDependencies": {
    "@swc/core": "1.13.3",
    "@types/react": "18.3.23",
    "react": "18.3.1",
    "swc-loader": "0.2.6",
    "typescript": "5.5.4",
    "webpack": "5.105.3",
    "webpack-cli": "6.0.1"
  }
}
